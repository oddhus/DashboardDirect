---
title: "Dynamic report"
output: html_document
params:
  plotArgs: NA
  insertions: NA
  removals: NA
---

```{r eval = FALSE, echo = FALSE}
# For PDF output, change the header to have "output: pdf_document".
#
# Note that due to an issue in rmarkdown, the default value of a parameter in
# the header cannot be `NULL`, so I used a default of `NA` for the default value
# of `n`.
```


# Tooth implant register

Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.


```{r plot, echo=FALSE, message=FALSE, fig.height=8, fig.width=12}
   for (i in 1:length(params$plotArgs)) {
     if(params$plotArgs[[i]]["tab"] == "Explorer"){
        print(exploreDataPlot(
       if(params$plotArgs[[i]]["dataset"] == "Insertions") {
          params$insertions
       } else {
         params$removals
       },
       FALSE,
       FALSE,
       params$plotArgs[[i]]["selectedClinic"],
       strsplit(params$plotArgs[[i]]["clinicCompare"], ";")[[1]],
       if (params$plotArgs[[i]]["allCombined"]) params$plotArgs[[i]]["fillColor"] else "Clinic",
       params$plotArgs[[i]]["facetRow"],
       strsplit(params$plotArgs[[i]]["specificFacetRow"], ";")[[1]],
       strsplit(params$plotArgs[[i]]["factorLevels"], ";")[[1]],
       params$plotArgs[[i]]["y"],
       params$plotArgs[[i]]["x"],
       params$plotArgs[[i]]["allCombined"]
     ))
     } else if (params$plotArgs[[i]]["tab"] == "Analyze") {
       print(
       analyzePlot(
          if(params$plotArgs[[i]]["dataset"] == "InsertionsAnalyze") {
          params$insertions
           } else {
             params$removals
           },
          params$plotArgs[[i]]["dependent"],
          strsplit(params$plotArgs[[i]]["numericIndependent"], ";")[[1]],
          strsplit(params$plotArgs[[i]]["factorIndependent"],";")[[1]],
          strsplit(params$plotArgs[[i]]["logicalIndependent"], ";")[[1]],
          strsplit(params$plotArgs[[i]]["highlight"], ";")[[1]],
          params$plotArgs[[i]]["analyzeMethod"]
        )
       )
     } else if (params$plotArgs[[i]]["tab"] == "Implant") {
       print(
       implantPlot(
          params$removals,
          strsplit(params$plotArgs[[i]]["RemovalReason"], ";")[[1]],
          strsplit(params$plotArgs[[i]]["ImplantName"], ";")[[1]],
          params$plotArgs[[i]]["ShowLotNr"]
        )
       )
     }
    
   }
#
 ```
